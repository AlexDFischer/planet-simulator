#include <stdio.h>
#include <stdlib.h>

#include "planets.h"

int main(int argc, char **argv)
{
	gravitySystem *system makeSystem(2);
	system->object[0].mass = 10000000000; /* a small asteroid */
	system->object[0].location[0] = 0;
	system->object[0].location[1] = 0;
	system->object[0].velocity[0] = 0;
	system->object[0].velocity[1] = 0;
	system->object[1].mass = 419600; /* this is the ISS, hopefully orbiting this asteroid */
	system->object
}


